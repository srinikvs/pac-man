{
  "id": "C19",
  "layer": "pixel",
  "title": "Pixel: ghosts/Pac not clipped at maze edges on start",
  "gate": "optional",
  "steps": [
    { "op": "openFresh" },
    { "op": "startPlay" }
  ],
  "expect": [
    { "assert": "mazeFullyVisible" },
    { "assert": "spritesInMaze" },
    { "assert": "noVerticalClip", "testId": ["maze", "stage"] }
  ]
}
