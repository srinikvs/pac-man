{
  "id": "B14",
  "layer": "e2e",
  "title": "Life / game-over path smoke",
  "gate": "optional",
  "steps": [
    { "op": "openFresh" },
    { "op": "startPlay" },
    { "op": "forceGameOver" }
  ],
  "expect": [
    { "assert": "visible", "testId": "gameover" },
    { "assert": "state", "value": "gameover" },
    { "assert": "visible", "testId": "retry" }
  ]
}
